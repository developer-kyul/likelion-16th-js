✨ 26-02-04 수요일- 오전

텍스트 / HTML 조작

## text 및 HTML 변경

- textContent : 요소 안의 "텍스트만" 변경 (HTML 태그는 문자로 처리됨)
- innerHTML : 요소 "내부"의 HTML 구조를 포함해 변경
- outerHTML : 요소 "자기 자신 + 내부" 전체를 HTML로 교체

```jsx
요소.textContent = "텍스트";
요소.innerHTML = "HTML 문자열";
요소.outerHTML = "HTML 문자열";
```

## DOM에 요소 추가

### 요소 생성

createElement : 요소 생성
이때 생성된 요소는 메모리에만 존재하며, 아직 화면(DOM)에는 나타나지 않습니다.

```
document.createElement('태그명')
```

### 요소 추가

appendChild() : 마지막 자식 요소로 추가
insertBefore() : 대상 요소 앞에 요소 삽입

```jsx
부모.appendChild(자식);
부모.insertBefore(새요소, 기준요소);
```

createElement  
appendChild  
children  
children.length  
textContent

## DOM에 여러 요소 추가

## 효율적인 요소 추가

복문 안에서 매번 DOM에 접근하는 것은 피해야 합니다.

## 다양한 추가 메서드

- .prepend() : 요소의 첫번째 자식 요소 뒤에 다른 요소(들) 삽입
- .append() : 요소의 마지막 자식 요소 뒤에 다른 요소(들) 삽입
- .before() : 요소 앞에 다른 요소(들) 삽입
- .after() : 요소 뒤에 다른 요소(들) 삽입
- .insertAdjacentElement() : 특정 위치에 요소 삽입
- .insertAdjacentHTML() : 특정 위치에 HTML 삽입

## IIFE

즉시 실행되는 함수 표현식

---

✨ 26-02-04 수요일- 오후

## 여러 요소 한 번에 추가하기

### innerHTML 활용

### 배열 → HTML 문자열 생성

### 기존 항목이 있는 경우

### DocumentFragment

## DOM에서 요소 제거

### 요소 제거

removeChild() : 부모를 기준으로 자식 요소를 제거해야 하는 옛 방식
remove() : 자기 자신을 바로 제거하는 최신 방식 (더 간단해서 사용도가 높다)

### 요소 이동

- .appendChild()
- .insertBefore()

<오늘날 사용가능>

- after
- before

🏷️ 부모요소 중심 메서드  
부모요소.appendChild()  
부모요소.insertBefore()  
부모요소.removeChild()

🏷️ 요소 중심 메서드  
요소.prepend()  
요소.append()  
요소.before()  
요소.after()  
요소.remove()

## switch 문

### 조건 스위치

```jsx
switch (변수) {
  case "값1":
    // 변수가 '값1'일 때 실행
    break;
  case "값2":
    // 변수가 '값2'일 때 실행
    break;
  default:
  // 일치하는 값이 없을 때 실행 (선택사항)
}
```

### 여러 조건 한 번에 처리

### 블록 (묶음) 처리

### switch vs. if 문
